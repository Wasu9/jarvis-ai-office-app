// JARVIS-SOURCE-FIDELITY-HARDENED-2026-09-04
// JARVIS-TASK-RUNTIME-HARDENED-2026-09-04
import { aiRegistry } from './ai/provider.js';
import { agentRegistry } from './agents/definitions.js';
import { memoryStore } from './memory.js';
import { generateDocxBuffer, DocxPaperData } from './docx-generator.js';
import { AttachedFile, ExecutionResult, GeneratedArtifact, TaskRecord, TaskStep, TaskStatus } from '../src/types/index.js';
import { saveTask } from './persistence.js';
import { validateExecutionResult, validateTaskRecord } from './core-qa.js';

export interface ExecuteTaskParams {
  userPrompt: string; selectedAgentId?: string; attachedFiles?: AttachedFile[]; model?: string;
  settings?: { instituteName?: string; targetExam?: string; defaultTargetExam?: string; primaryLanguage?: string; aiModel?: string };
  onStep?: (step: TaskStep) => void; resumeFrom?: number; resumeQuestions?: any[];
  sourceTotalQuestions?: number; sourceRange?: { start: number; end: number };
}

const PAPER_SCHEMA = {
  type: 'OBJECT',
  properties: {
    title: { type: 'STRING' }, instituteName: { type: 'STRING' }, className: { type: 'STRING' }, examType: { type: 'STRING' },
    subject: { type: 'STRING' }, date: { type: 'STRING' }, duration: { type: 'STRING' }, totalMarks: { type: 'STRING' }, medium: { type: 'STRING' },
    chapterName: { type: 'STRING' }, syllabus: { type: 'STRING' }, instructions: { type: 'ARRAY', items: { type: 'STRING' } },
    questions: { type: 'ARRAY', items: { type: 'OBJECT', properties: {
      number: { type: 'STRING' }, sectionName: { type: 'STRING' }, questionType: { type: 'STRING', enum: ['mcq','numerical','unknown'] },
      textEn: { type: 'STRING' }, textHi: { type: 'STRING' },
      options: { type: 'ARRAY', items: { type: 'OBJECT', properties: { en: { type: 'STRING' }, hi: { type: 'STRING' } }, required: ['en','hi'] } },
      correctOption: { type: 'STRING' }, solution: { type: 'STRING' }, diagramSvg: { type: 'STRING' }, sourcePage: { type: 'INTEGER' },
    }, required: ['number','questionType','textEn','textHi','options'] } },
  }, required: ['title','questions'],
};

const META_SCHEMA = { type: 'OBJECT', properties: {
  title:{type:'STRING'}, instituteName:{type:'STRING'}, className:{type:'STRING'}, examType:{type:'STRING'}, subject:{type:'STRING'}, date:{type:'STRING'},
  duration:{type:'STRING'}, totalMarks:{type:'STRING'}, medium:{type:'STRING'}, chapterName:{type:'STRING'}, syllabus:{type:'STRING'}, instructions:{type:'ARRAY',items:{type:'STRING'}},
}, required:['title','instituteName','className','examType','subject','date','duration','totalMarks','medium','chapterName','syllabus','instructions'] };

function isPaperAgent(id:string){return id==='neet-jee-paper'||id==='dpp-generator'||id==='pdf-bilingual';}
function sourceConversion(prompt:string,files?:AttachedFile[]){const p=prompt.toLowerCase();return !!files?.length&&/bilingual|translate|translation|convert|extract|read|exact|same|faithful|preserve|hindi|अनुवाद|बाइलिंगुअल|जैसा है|सिर्फ|word format|docx/.test(p);}
function wantsKey(prompt:string){return /answer\s*key|उत्तर\s*कुंजी/i.test(prompt);}
function wantsSolutions(prompt:string){return /solution|solutions|step[- ]by[- ]step|विस्तृत हल|हल सहित/i.test(prompt);}
function extractJson(text:string):any{const cleaned=text.replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/i,'').trim();try{return JSON.parse(cleaned);}catch{}const a=cleaned.indexOf('{'),b=cleaned.lastIndexOf('}');if(a>=0&&b>a){try{return JSON.parse(cleaned.slice(a,b+1));}catch{}}throw new Error('JARVIS received invalid structured data and stopped before creating the Word file.');}
function countRequested(prompt:string):number|null{const m=prompt.match(/\b(\d{1,3})\s*(?:questions?|qs?|प्रश्न)\b/i);return m?Number(m[1]):null;}
function sleep(ms:number){return new Promise(r=>setTimeout(r,ms));}\n\nfunction sourceQuestionReference(files: AttachedFile[] | undefined, start: number, end: number): string {\n  const source = (files || []).map(f => typeof f.textPreview === 'string' ? f.textPreview : '').filter(Boolean).join('\\n\\n');\n  if (!source.trim()) return '';\n  const matches = [...source.matchAll(/(?:^|\\n)\\s*(?:Q(?:uestion)?\\s*\\.?\\s*)?(\\d{1,3})[.)]\\s+/gi)];\n  let runIndex = -1, runLength = 0;\n  for (let i = 0; i < matches.length; i++) {\n    if (Number(matches[i][1]) !== 1) continue;\n    let expected = 1, j = i;\n    while (j < matches.length && Number(matches[j][1]) === expected) { expected++; j++; }\n    if (expected - 1 > runLength) { runIndex = i; runLength = expected - 1; }\n  }\n  if (runIndex < 0) return '';\n  const ranges = new Map<number, { start: number; end: number }>();\n  for (let j = runIndex; j < runIndex + runLength; j++) {\n    const n = Number(matches[j][1]);\n    ranges.set(n, { start: matches[j].index, end: j + 1 < matches.length ? matches[j + 1].index : source.length });\n  }\n  const first = ranges.get(start), last = ranges.get(end);\n  return first && last ? source.slice(first.start, last.end).slice(0, 24000).trim() : '';\n}\n
function normalizeOption(n:string){const s=String(n||'').trim().toUpperCase();return ({A:'1',B:'2',C:'3',D:'4'} as Record<string,string>)[s]||s;}
function visualLikely(q:any){return /(figure|fig\.|graph|plot|diagram|image|shown below|given below|चित्र|आरेख|ग्राफ|नीचे दिया|नीचे दिए)/i.test(`${q.textEn||''} ${q.textHi||''}`);}

function validate(data:DocxPaperData,expectedTotal:number,sourceLocked:boolean){
  const qs=data.questions||[];if(!qs.length)throw new Error('No questions were produced.');
  if(sourceLocked&&qs.length!==expectedTotal)throw new Error(`Question count mismatch: source has ${expectedTotal}, extracted ${qs.length}.`);
  const seen=new Set<number>();
  for(const q of qs){
    const n=Number(String(q.number).replace(/^Q\.?/i,''));if(!Number.isInteger(n))throw new Error(`Invalid question number: ${q.number}`);if(seen.has(n))throw new Error(`Duplicate question Q.${n}.`);seen.add(n);
    if(!q.textEn?.trim()||!q.textHi?.trim())throw new Error(`Q.${n} is missing English or Hindi text.`);
    const numerical=q.questionType==='numerical';
    if(numerical){if((q.optionsEn?.length||0)!==0||(q.optionsHi?.length||0)!==0)throw new Error(`Q.${n} is Numerical Value Answer Type but contains MCQ options.`);}
    else{if((q.optionsEn?.length||0)!==4||(q.optionsHi?.length||0)!==4)throw new Error(`Q.${n} must contain exactly four English and four Hindi options.`);for(let i=0;i<4;i++)if(!q.optionsEn?.[i]?.trim()||!q.optionsHi?.[i]?.trim())throw new Error(`Q.${n} option ${i+1} is missing a language value.`);}
    // A missing model-rendered diagram must never abort an otherwise valid source chunk.
    // Keep the source question and continue; visual fidelity can be reported separately.
    if(sourceLocked&&visualLikely(q)&&!q.diagramSvg?.trim())console.warn(`[Source QA] Q.${n} references a visual but diagramSvg was not captured; continuing without aborting the mission.`);
  }
}

export class TaskRunner{
 static routeAgent(prompt:string,files?:AttachedFile[],manual?:string):string{
  if(manual&&manual!=='auto'){const a=agentRegistry.getAgent(manual);if(a?.enabled)return a.id;}
  const lower=prompt.toLowerCase();if(sourceConversion(prompt,files))return'pdf-bilingual';
  const hasPdf=files?.some(f=>f.type.includes('pdf')||f.name.toLowerCase().endsWith('.pdf'));
  if(['notice','poster','circular','ptm','holiday','vacation','announcement','seminar'].some(k=>lower.includes(k)))return'poster-notice';
  if(['reel','short','video script','shot list','voiceover','60 second','30 second','video concept'].some(k=>lower.includes(k)))return'reel-content';
  if(['instagram','facebook','youtube community','caption','hashtag','carousel','social media','admission open','promo post'].some(k=>lower.includes(k)))return'social-media';
  if(['dpp','daily practice','practice problem','homework sheet','practice sheet'].some(k=>lower.includes(k)))return'dpp-generator';
  if(hasPdf||lower.includes('pdf')||(lower.includes('read')&&!!files?.length))return'pdf-bilingual';
  if(['neet','jee','question paper','test paper','exam paper','questions','physics','chemistry','biology','botany','zoology','maths','mathematics'].some(k=>lower.includes(k)))return'neet-jee-paper';
  const custom=agentRegistry.getAllAgents().find(a=>a.isCustom&&a.enabled&&lower.includes(a.name.toLowerCase()));if(custom)return custom.id;
  const words=prompt.trim().replace(/\s+/g,' ').split(' ').slice(0,6).join(' ');const existing=agentRegistry.getAllAgents().find(a=>a.isCustom&&a.enabled&&a.description.toLowerCase().includes(`auto-hired for: ${words.toLowerCase()}`));if(existing)return existing.id;
  return agentRegistry.addCustomAgent({name:`Specialist — ${words.slice(0,42)}`,description:`Auto-hired for: ${words}`,capabilities:['custom'],systemPrompt:`You are a JARVIS specialist created for this request: ${prompt}. Follow exact user requirements, preserve source content, do not invent missing facts, and return production-ready work.`,inputRequirements:['User instructions','Optional documents'],outputTypes:['markdown','docx'],category:'custom',enabled:true,iconName:'Bot',samplePrompts:[prompt]}).id;
 }
 static async execute(params:ExecuteTaskParams):Promise<TaskRecord>{
  const id=`task-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,start=Date.now(),steps:TaskStep[]=[];const add=(status:TaskStatus,label:string,details?:string)=>{const s={status,label,timestamp:new Date().toISOString(),details};steps.push(s);try{params.onStep?.(s);}catch{}};
  let agent:any;let resumeCheckpoint:any=undefined;const fail=(message:string)=>{add('failed',message);const task=this.failed(id,start,params,agent,steps,message,resumeCheckpoint);try{validateTaskRecord(task);saveTask(task);}catch{}return task;};
  add('waiting',params.resumeFrom&&params.resumeFrom>1?`JARVIS resuming from Q.${params.resumeFrom}`:'JARVIS command received');const locked=sourceConversion(params.userPrompt,params.attachedFiles);const agentId=this.routeAgent(params.userPrompt,params.attachedFiles,params.selectedAgentId);agent=agentRegistry.getAgent(agentId)||agentRegistry.getAgent('neet-jee-paper')!;
  add('understanding',`Intent understood · ${agent.name}`,`Selected Agent ID: ${agent.id}`);add('working',locked?'SOURCE LOCK ACTIVE · uploaded document is authoritative':'Task parameters validated');
  const memory=memoryStore.getMemoryPromptContext(),configuredInstitute=params.settings?.instituteName||'',configuredExam=params.settings?.targetExam||params.settings?.defaultTargetExam||'',structured=isPaperAgent(agent.id);
  const lockRules=locked?`\nSOURCE-LOCK MODE — DOCUMENT CONVERSION ONLY\n- The attached PDF is the sole source of truth. Never invent, correct, summarize, merge, reorder or silently improve source content.\n- Copy source title, institute, class, exam, subject, date, duration, marks, medium, chapter/syllabus text and every printed instruction. If a field is absent, leave it blank; NEVER invent a chapter name.\n- Preserve every question number and every source question. Do not assume a standard 180-question exam.\n- Detect section type from the PDF. MCQ sections have exactly four options. Numerical Value Answer Type sections have NO options; return options as an empty array.\n- Preserve English source wording faithfully. Hindi is a faithful translation only.\n- EVERY mathematical expression MUST be enclosed in [[MATH:...]] markers. Inside the marker use LaTeX only: \\frac{...}{...}, ^{...}, _{...}, \\sqrt{...}, \\alpha, \\mu, \\theta, \\times, \\pm, \\propto. Never flatten, solve, simplify or rewrite a formula.\n- Preserve units exactly, including m/s^2, kg m/s, Hz, Å, %, N, J and scientific notation.\n- If a source question references a figure/graph/diagram/image, diagramSvg is mandatory. Reproduce visible geometry, axes, arrows, labels and important text as closely as possible and return sourcePage.\n` : '';
  const system=`${agent.systemPrompt}\n\n${memory}\n\nJARVIS PRODUCTION RULES:\n- Never invent or silently correct source-derived content.\n- Validate semantic fidelity after structured extraction.${lockRules}\n${structured?'Return ONLY valid JSON matching the schema. No markdown fences or commentary.':''}`;
  add('generating',locked?'Source extraction · fidelity mode':`Generating with ${params.model||'gemini-3.7-flash'}`);
  const provider=aiRegistry.getProvider('gemini');const inline=(params.attachedFiles||[]).filter(f=>!!f.base64Data).map(f=>({mimeType:f.type||'application/pdf',data:String(f.base64Data).replace(/^data:[^;]+;base64,/,'')}));let raw='';let generationError='';
  try{
   if(structured&&locked&&inline.length){
    const totalQ=Number(params.sourceTotalQuestions||countRequested(params.userPrompt)||0);if(!Number.isInteger(totalQ)||totalQ<1)throw new Error('JARVIS refused to guess the question count. Source question count is required.');
    const range=params.sourceRange||{start:Math.min(totalQ,Math.max(1,Number(params.resumeFrom||1))),end:totalQ};const initialQuestions=Array.isArray(params.resumeQuestions)?params.resumeQuestions:[];
    resumeCheckpoint={completedQuestions:Math.max(0,range.start-1),totalQuestions:totalQ,nextQuestion:range.start,questions:[...initialQuestions]};
    add('working','SOURCE HEADER · reading exam metadata and instructions');
    const metaRaw=await provider.generateText('Inspect the attached PDF and extract ONLY the source document header/cover metadata: exact title, institute name, class, exam type, subject, date, duration, maximum marks, medium, chapter name if explicitly printed, syllabus/chapter text if explicitly printed, and every printed instruction. Do not infer missing fields. Return blank strings for fields not printed.',{systemInstruction:system,model:params.model||'gemini-3.7-flash',responseMimeType:'application/json',responseSchema:META_SCHEMA,inlineFiles:inline});
    const meta=extractJson(metaRaw);const merged:any={title:String(meta.title||''),instituteName:String(meta.instituteName||''),className:String(meta.className||''),examType:String(meta.examType||''),subject:String(meta.subject||''),date:String(meta.date||''),duration:String(meta.duration||''),totalMarks:String(meta.totalMarks||''),medium:String(meta.medium||''),chapterName:String(meta.chapterName||''),syllabus:String(meta.syllabus||''),instructions:Array.isArray(meta.instructions)?meta.instructions:[],questions:[...initialQuestions]};
    const chunkSize=10,chunks:Array<{start:number;end:number}>=[];for(let s=range.start;s<=range.end;s+=chunkSize)chunks.push({start:s,end:Math.min(s+chunkSize-1,range.end)});
    const extractChunk=async(c:{start:number;end:number})=>{let lastError='';for(let attempt=1;attempt<=1;attempt++){try{add('working',`SOURCE EXTRACTION · Q.${c.start}–Q.${c.end}`,`Attempt ${attempt}/1`);const sourceReference=sourceQuestionReference(params.attachedFiles,c.start,c.end);const chunkRaw=await provider.generateText(`Extract ONLY source questions Q.${c.start} through Q.${c.end} from the attached PDF. The complete source contains exactly ${totalQ} numbered questions. Read the actual PDF visually and textually. Preserve every visible word boundary and space; never concatenate adjacent tokens. Preserve punctuation, decimals, minus signs, fractions, superscripts, subscripts, Greek letters, units and symbols exactly. If OCR text is visually joined, restore the correct spacing from the rendered PDF. Return exactly ${c.end-c.start+1} questions in exact source order. For MCQ questions return exactly four options. For Numerical Value Answer Type questions return options as an empty array. Preserve every mathematical expression using [[MATH:LaTeX]] markers. Preserve units and scientific notation. If a question has a graph/figure/diagram/image, populate diagramSvg with a self-contained SVG whenever a real graph/figure/diagram is visible; never put placeholder text into diagramSvg. Include sourcePage. Do not solve, correct, summarize, merge, skip or renumber anything.\n\nAUTHORITATIVE PDF TEXT LAYER — extracted from the same uploaded PDF before AI generation. Use it to recover every formula, fraction, superscript/subscript, vector, Greek symbol, unit, number, punctuation mark and word boundary. Never omit an expression because it is difficult to OCR. Do not solve or simplify.\n<<<SOURCE TEXT>>>\n${sourceReference}\n<<<END SOURCE TEXT>>>`,{systemInstruction:system,model:params.model||'gemini-3.7-flash',responseMimeType:'application/json',responseSchema:PAPER_SCHEMA,inlineFiles:inline});const chunk=extractJson(chunkRaw);const questions=Array.isArray(chunk.questions)?chunk.questions:[];if(questions.length!==c.end-c.start+1)throw new Error(`Incomplete batch Q.${c.start}–Q.${c.end}: received ${questions.length}.`);const nums=questions.map((q:any)=>Number(String(q.number||'').replace(/^Q\.?/i,'')));for(let i=0;i<nums.length;i++)if(nums[i]!==c.start+i)throw new Error(`Numbering mismatch: expected Q.${c.start+i}, received Q.${nums[i]||'?'}.`);return questions;}catch(e:any){lastError=e?.message||'Generation failed';if(attempt<3){add('working',`Retrying source batch Q.${c.start}–Q.${c.end}`,lastError);await sleep(1000*attempt);}}}throw new Error(`Source extraction failed for Q.${c.start}–Q.${c.end}: ${lastError}`);};
    const generated: any[]=[...initialQuestions];for(const c of chunks){const q=await extractChunk(c);generated.push(...q);merged.questions=[...generated];}
    raw=JSON.stringify(merged);
   }else{raw=await provider.generateText(params.userPrompt,{systemInstruction:system,model:params.model||'gemini-3.7-flash',responseMimeType:structured?'application/json':undefined,responseSchema:structured?PAPER_SCHEMA:undefined,inlineFiles:inline});}
  }catch(e:any){generationError=e?.message||'Generation failed';}
  if(generationError)return fail(`Generation failed: ${generationError}`);add('checking',locked?'Running source-fidelity + bilingual QA':'Running structural + output QA');let data:DocxPaperData;
  try{data=structured?this.fromJson(extractJson(raw),params.userPrompt,configuredInstitute,configuredExam,locked):{title:params.userPrompt.slice(0,60),instituteName:configuredInstitute,examType:configuredExam,rawContent:raw};if(structured){const chunkRange=params.sourceRange;const chunkBase=Array.isArray(params.resumeQuestions)?params.resumeQuestions.length:0;const chunkSize=chunkRange?Math.max(0,chunkRange.end-chunkRange.start+1):0;const expected=locked?(chunkRange?chunkBase+chunkSize:Number(params.sourceTotalQuestions||countRequested(params.userPrompt)||0)):Number(countRequested(params.userPrompt)||data.questions?.length||0);validate(data,expected||data.questions?.length||0,locked);if(locked)this.validateSequence(data,expected,chunkRange?.start||1);}}catch(e:any){return fail(`QA failed: ${e.message}`);}
  const artifacts:GeneratedArtifact[]=[];try{const buf=await generateDocxBuffer(data);artifacts.push({id:`art-${Date.now()}-1`,name:`${agent.shortCode}_${Date.now()}.docx`,fileType:'docx',docxBase64:buf.toString('base64'),size:buf.length,metadata:{title:data.title,questionsCount:data.questions?.length||0}});}catch(e:any){return fail(`DOCX generation failed: ${e.message}`);}
  artifacts.push({id:`art-${Date.now()}-2`,name:`${agent.shortCode}_content.json`,fileType:'json',content:structured?JSON.stringify(data,null,2):raw,size:Buffer.byteLength(structured?JSON.stringify(data):raw,'utf8')});
  const result:ExecutionResult={summary:`Successfully executed by ${agent.name}`,rawText:raw,structuredData:data,artifacts,agentUsed:{id:agent.id,name:agent.name},metrics:{durationMs:Date.now()-start}};try{validateExecutionResult(result,locked);}catch(e:any){return fail(e.message);}add('completed',locked?'Source-faithful bilingual document ready':'Task completed successfully');const task:TaskRecord={id,title:params.userPrompt.slice(0,70),userPrompt:params.userPrompt,agentId:agent.id,agentName:agent.name,status:'completed',createdAt:new Date(start).toISOString(),completedAt:new Date().toISOString(),steps,attachedFiles:params.attachedFiles||[],result};try{validateTaskRecord(task);saveTask(task);}catch(e:any){return fail(`Task persistence/validation failed: ${e.message}`);}return task;
 }
 private static validateSequence(data:DocxPaperData,expected:number,startAt=1){const qs=[...(data.questions||[])].map(q=>Number(String(q.number).replace(/^Q\.?/i,''))).filter(Number.isFinite).sort((a,b)=>a-b);if(qs.length!==expected)throw new Error(`Expected ${expected} questions, found ${qs.length}.`);for(let i=0;i<qs.length;i++)if(qs[i]!==startAt+i)throw new Error(`Source question sequence has a gap at Q.${startAt+i}; found Q.${qs[i]}.`);}
 private static fromJson(data:any,prompt:string,configuredInstitute:string,configuredExam:string,locked:boolean):DocxPaperData{return{title:String(data.title||prompt.slice(0,60)),instituteName:String(data.instituteName||(!locked?configuredInstitute:'')),className:String(data.className||''),examType:String(data.examType||(!locked?configuredExam:'')),subject:String(data.subject||''),date:String(data.date||''),duration:String(data.duration||''),totalMarks:data.totalMarks??'',medium:String(data.medium||''),chapterName:String(data.chapterName||''),syllabus:String(data.syllabus||''),instructions:Array.isArray(data.instructions)?data.instructions.map((x:any)=>String(x)):[],includeAnswerKey:!locked&&wantsKey(prompt),includeSolutions:!locked&&wantsSolutions(prompt),questions:data.questions.map((q:any,i:number)=>({number:String(q.number||i+1).replace(/^Q\.?/i,''),sectionName:String(q.sectionName||''),questionType:q.questionType==='numerical'?'numerical':q.questionType==='mcq'?'mcq':'unknown',textEn:String(q.textEn||'').trim(),textHi:String(q.textHi||'').trim(),optionsEn:Array.isArray(q.options)?q.options.map((o:any)=>String(o?.en||'').trim()):[],optionsHi:Array.isArray(q.options)?q.options.map((o:any)=>String(o?.hi||'').trim()):[],correctOption:normalizeOption(String(q.correctOption||'')),solution:String(q.solution||'').trim(),diagramSvg:typeof q.diagramSvg==='string'&&q.diagramSvg.trim()?q.diagramSvg.trim():undefined}))};}
 private static failed(id:string,start:number,params:ExecuteTaskParams,agent:any,steps:TaskStep[],error:string,checkpoint?:any):TaskRecord{return{id,title:params.userPrompt.slice(0,60),userPrompt:params.userPrompt,agentId:agent?.id||'jarvis',agentName:agent?.name||'JARVIS',status:'failed',createdAt:new Date(start).toISOString(),completedAt:new Date().toISOString(),steps,attachedFiles:params.attachedFiles||[],error,checkpoint};}
}