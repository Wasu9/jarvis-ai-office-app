import React, { useRef, useState } from 'react';
import { ArrowUp, FileText, MessageSquare, RotateCcw, X, Brain, CheckCircle2, Loader2 } from 'lucide-react';
import { AgentDefinition, AttachedFile, JarvisSettings, TaskRecord } from '../types';
import { FileDropzone } from './FileDropzone';
import { VoiceAssistant } from './VoiceAssistant';
import { TaskPipelineStepper } from './TaskPipelineStepper';
import { ResultViewer } from './ResultViewer';
import { OfficeSimulation3DHyperReal as OfficeSimulation3D } from './OfficeSimulation3DHyperReal';
import { ApiService } from '../services/api';
interface Props { agents: AgentDefinition[]; selectedAgentId: string; setSelectedAgentId: (id: string) => void; settings: JarvisSettings; activeTask: TaskRecord | null; setActiveTask: (t: TaskRecord | null) => void; onTaskCompleted: (t: TaskRecord) => void; onAgentHired?: () => void; recentTasks: TaskRecord[]; }
type Msg = { id: string; role: 'user' | 'assistant'; content: string };
type ChatHistory = Array<{role:'user'|'assistant';content:string}>;
type MissionPlan = { agentName: string; agentId: string; summary: string; steps: string[] };
const quick=[['NEET Paper','Create a NEET Physics paper with bilingual English-Hindi questions and options.'],['PDF to Word','Read the attached PDF and convert it into a clean bilingual Word document.'],['DPP','Create a NEET DPP with questions, answer key and detailed solutions.'],['Academy Notice','Create a professional Shaheen Academy Jaipur notice for an upcoming mock test.']];
export const OfficeAssistant:React.FC<Props>=(p)=>{const{agents,selectedAgentId,setSelectedAgentId,settings,activeTask,setActiveTask,onTaskCompleted,onAgentHired,recentTasks}=p;const[messages,setMessages]=useState<Msg[]>([{id:'welcome',role:'assistant',content:'Good to see you, Boss. 👋 The office is open. Give me a mission normally — I will route it to the right employee.'}]);const[prompt,setPrompt]=useState('');const[files,setFiles]=useState<AttachedFile[]>([]);const[busy,setBusy]=useState(false);const[planning,setPlanning]=useState(false);const[plan,setPlan]=useState<MissionPlan|null>(null);const[advanced,setAdvanced]=useState(false);const ref=useRef<HTMLTextAreaElement>(null);const enabled=agents.filter(a=>a.enabled);const completed=recentTasks.filter(t=>t.status==='completed').length;
const reset=()=>{setMessages([{id:Date.now().toString(),role:'assistant',content:'Fresh office session. ☕ Everyone is back at their desks. What shall we work on?'}]);setPrompt('');setFiles([]);setPlan(null);setActiveTask(null)};
const execute=async(userText:string,history:ChatHistory)=>{setBusy(true);const pending:TaskRecord={id:`p${Date.now()}`,title:userText.slice(0,90),userPrompt:userText,agentId:selectedAgentId==='auto'?'jarvis':selectedAgentId,agentName:selectedAgentId==='auto'?'JARVIS':(enabled.find(a=>a.id===selectedAgentId)?.name||'JARVIS'),status:'understanding',createdAt:new Date().toISOString(),steps:[{status:'understanding',label:'JARVIS is understanding the mission',timestamp:new Date().toISOString()}],attachedFiles:[...files]};setActiveTask(pending);try{const t=await ApiService.executeTask({userPrompt:userText,selectedAgentId,attachedFiles:files,model:settings.aiModel,settings,history});setActiveTask(t);if(t.agentId==='conversational-core'){setMessages(m=>[...m,{id:`a${Date.now()}`,role:'assistant',content:t.result?.rawText||'I am ready, Boss.'}]);setActiveTask(null)}else{if(t.steps?.some(s=>s.label.toLowerCase().includes('hr hired')))onAgentHired?.();onTaskCompleted(t)}}catch(e:any){setActiveTask({...pending,status:'failed',error:e?.message||'Something went wrong',completedAt:new Date().toISOString()})}finally{setBusy(false);setFiles([])}};
const send=async(custom?:string)=>{const text=(custom??prompt).trim();if(!text&&!files.length)return;const userText=text||'Process the attached file.';const history=messages.slice(-12).map(m=>({role:m.role,content:m.content}));setMessages(m=>[...m,{id:`u${Date.now()}`,role:'user',content:userText}]);setPrompt('');setPlanning(true);setPlan(null);try{const route=await ApiService.checkAgentRoute(userText,files,selectedAgentId);const routed=route?.agent||{};const routedId=route?.routedAgentId||selectedAgentId||'jarvis';const routedName=routed?.name||enabled.find(a=>a.id===routedId)?.name||'JARVIS';const summary=route?.reason||route?.message||`JARVIS will route this mission to ${routedName}.`;const production=/(paper|question|test|exam|dpp|pdf|word|docx|translate|poster|notice|circular|reel|video|social|instagram|facebook|youtube|worksheet|answer key|document|mock test)/i.test(userText)||files.length>0;const steps=production?['Understand the request and required files','Assign the most suitable AI employee','Execute the task with the selected tools','Verify the output before delivery']:['Understand your request','Choose the appropriate JARVIS capability','Prepare the response','Deliver the result'];setPlan({agentId:routedId,agentName:routedName,summary,steps});}catch(e){const routedName=selectedAgentId==='auto'?'JARVIS':(enabled.find(a=>a.id===selectedAgentId)?.name||'JARVIS');setPlan({agentId:selectedAgentId||'jarvis',agentName:routedName,summary:'Routing service is temporarily unavailable; JARVIS will use its normal automatic routing during execution.',steps:['Understand the request','Use normal JARVIS automatic routing','Execute the task','Verify and deliver the result']});}finally{setPlanning(false)}};
const startPlannedMission=()=>{if(!plan)return;const userText=messages[messages.length-1]?.content;if(!userText)return;const history=messages.slice(0,-1).slice(-12).map(m=>({role:m.role,content:m.content}));setPlan(null);void execute(userText,history)};
return <div className="mx-auto w-full max-w-[1500px] text-slate-200"><header className="mb-3 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-slate-700 bg-[#0c141b] px-4 py-3 shadow-lg"><div><div className="flex items-center gap-2 text-sm font-black text-white">🏢 SHAHEEN AI OFFICE <span className="rounded-full bg-emerald-400/10 px-2 py-0.5 text-[8px] text-emerald-300">LIVE · 3D</span></div><div className="text-[9px] text-slate-500">Shaheen Academy Jaipur · {enabled.length} AI employees · {completed} missions completed</div></div><button onClick={reset} className="rounded-lg border border-slate-700 px-3 py-1.5 text-xs text-slate-400 hover:border-cyan-300/30 hover:text-white"><RotateCcw className="mr-1 inline h-3 w-3"/>Reset Office</button></header><OfficeSimulation3D agents={enabled} activeTask={activeTask}/>{planning&&<section className="mt-3 rounded-2xl border border-cyan-300/20 bg-[#08121a] p-4 shadow-xl"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Loader2 className="h-5 w-5 animate-spin"/></div><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-cyan-200">STEP 2 · PLANNING MISSION</div><div className="mt-1 text-xs text-slate-500">JARVIS is analysing the request and selecting the right employee.</div></div></div></section>}{plan&&<section className="mt-3 overflow-hidden rounded-2xl border border-cyan-300/20 bg-[#08121a] shadow-xl"><div className="flex items-center justify-between border-b border-white/5 px-4 py-3"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Brain className="h-4 w-4"/></div><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-cyan-200">STEP 2 · MISSION PLAN</div><div className="text-[9px] text-slate-500">Plan ready before execution</div></div></div><span className="rounded-full border border-emerald-400/20 bg-emerald-400/10 px-2 py-1 text-[8px] font-bold text-emerald-300">READY</span></div><div className="grid gap-4 p-4 md:grid-cols-[.8fr_1.2fr]"><div className="rounded-xl border border-white/5 bg-black/20 p-3"><div className="text-[8px] font-bold uppercase tracking-[.18em] text-slate-600">ASSIGNED EMPLOYEE</div><div className="mt-2 text-sm font-bold text-white">{plan.agentName}</div><div className="mt-1 text-[9px] text-slate-500">{plan.summary}</div></div><div className="rounded-xl border border-white/5 bg-black/20 p-3"><div className="mb-2 text-[8px] font-bold uppercase tracking-[.18em] text-slate-600">EXECUTION PLAN</div><div className="space-y-2">{plan.steps.map((s,i)=><div key={s} className="flex items-center gap-2 text-[10px] text-slate-300"><span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full border border-cyan-300/20 bg-cyan-300/5 text-[8px] text-cyan-200">{i+1}</span>{s}</div>)}</div></div></div><div className="flex justify-end gap-2 border-t border-white/5 px-4 py-3"><button onClick={()=>setPlan(null)} className="rounded-xl border border-slate-700 px-4 py-2 text-[10px] text-slate-400">Cancel</button><button onClick={startPlannedMission} className="rounded-xl bg-cyan-300 px-4 py-2 text-[10px] font-black text-slate-950"><CheckCircle2 className="mr-1 inline h-3.5 w-3.5"/>START MISSION</button></div></section>}{activeTask&&<section className="mt-3 rounded-2xl border border-cyan-300/15 bg-[#0b131a] p-3 shadow-xl"><div className="mb-2 flex items-center justify-between"><div className="text-[10px] font-bold uppercase tracking-[.18em] text-cyan-200">LIVE MISSION · {activeTask.agentName}</div><div className={`rounded-full px-2 py-1 text-[8px] font-bold ${activeTask.status==='failed'?'bg-red-400/10 text-red-300':activeTask.status==='completed'?'bg-emerald-400/10 text-emerald-300':'bg-amber-300/10 text-amber-200'}`}>{activeTask.status.toUpperCase()}</div></div><TaskPipelineStepper task={activeTask} onRetry={()=>void execute(activeTask.userPrompt,messages.slice(-12).map(m=>({role:m.role,content:m.content})))}/>{activeTask.status==='completed'&&activeTask.result&&<div className="mt-3 border-t border-white/10 pt-3"><ResultViewer task={activeTask} onRepeat={()=>setPrompt(activeTask.userPrompt)}/></div>}</section>}<section className="mt-3 rounded-2xl border border-slate-700 bg-[#0c141b] p-3 shadow-xl"><div className="flex items-end gap-2"><div className="min-w-0 flex-1 rounded-2xl border border-slate-700 bg-[#0a1118] focus-within:border-cyan-300/40">{files.length>0&&<div className="flex flex-wrap gap-2 px-3 pt-3">{files.map(f=><div key={f.id} className="rounded-lg border border-cyan-400/15 px-2 py-1 text-[9px] text-cyan-200"><FileText className="mr-1 inline h-3 w-3"/>{f.name}<button onClick={()=>setFiles(x=>x.filter(y=>y.id!==f.id))}><X className="ml-1 inline h-3 w-3"/></button></div>)}</div>}<textarea ref={ref} value={prompt} onChange={e=>setPrompt(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();void send()}}} rows={2} placeholder="Boss, what should the office work on?" className="w-full resize-none bg-transparent px-4 py-3 text-sm text-white outline-none placeholder:text-slate-600" disabled={busy||planning}/><div className="flex items-center gap-2 px-3 pb-2"><FileDropzone attachedFiles={files} onAddFiles={x=>setFiles(v=>[...v,...x])} onRemoveFile={id=>setFiles(v=>v.filter(x=>x.id!==id))} disabled={busy||planning}/><VoiceAssistant onSpeechResult={t=>setPrompt(x=>x?`${x} ${t}`:t)} onCommandReady={t=>void send(t)} isProcessing={busy||planning}/>{advanced&&<select value={selectedAgentId} onChange={e=>setSelectedAgentId(e.target.value)} className="rounded-lg border border-slate-700 bg-[#111923] px-2 py-1.5 text-[9px] text-slate-400"><option value="auto">JARVIS decides</option>{agents.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select>}<button onClick={()=>setAdvanced(v=>!v)} className="ml-auto text-[9px] text-slate-600 hover:text-cyan-200">{advanced?'Hide controls':'Controls'}</button></div></div><button onClick={()=>void send()} disabled={busy||planning||(!prompt.trim()&&!files.length)} className="flex h-12 w-12 shrink-0 items-center justify-center rounded-2xl bg-cyan-300 text-slate-950 disabled:opacity-30"><ArrowUp className="h-5 w-5"/></button></div><div className="mt-2 flex flex-wrap gap-2">{quick.map(([label,text])=><button key={label} onClick={()=>{setPrompt(text);ref.current?.focus()}} className="rounded-full border border-slate-700 bg-white/[.02] px-3 py-1.5 text-[9px] text-slate-400 hover:border-cyan-300/25 hover:text-cyan-200">{label}</button>)}<span className="ml-auto text-[9px] text-slate-600"><MessageSquare className="mr-1 inline h-3 w-3"/>Talk naturally with JARVIS</span></div></section></div>};